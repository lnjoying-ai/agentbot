spring:
  application:
    name: agentbot
  config:
    import: "optional:file:${AGENTBOT_CONFIG:./config/agentbot.yml}"

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true







